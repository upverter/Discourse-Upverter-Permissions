en:
  site_settings:
    upverter_domain: 'The domain name of your Upverter instance. Defaults to: "upverter.com"'
    upverter_permissions_category: 'The name of the category where topics live that require permssions from Upverter. Defaults to the Embedding category.'
    upverter_cache_bypass_subdomain: 'If a subdomain is needed to bypass caching for the permission check, add it here, with a trailing period. Otherwise, leave empty. (No spaces!)'
